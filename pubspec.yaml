name: book_rsvp_reader
description: "Speed-read physical books using camera OCR and RSVP technique"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # TODO: Uncomment when ready to implement
  # camera: ^0.11.0
  # google_mlkit_text_recognition: ^0.12.0
  # path_provider: ^2.1.0
  # path: ^1.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
